# Print Server Environment Configuration

# Server Port (default: 3001)
PORT=3001

# Environment mode: development or production
NODE_ENV=production

# Printer Type: USB, Serial, or Network
PRINTER_TYPE=USB

# For Serial printers (uncomment if using serial)
# SERIAL_PORT=COM3

# For Network printers (uncomment if using network/WiFi printer)
# PRINTER_IP=192.168.1.100
# PRINTER_PORT=9100

# CORS allowed origins (for security, default allows all)
# ALLOWED_ORIGINS=http://localhost:3000,https://your-app-url.com
